# Quick Start

1. Ensure MongoDB is running.
2. Copy `.env.example` to `.env` in `backend/server`.
3. Install dependencies from project root:
   - `npm install`
4. Run backend only:
   - `npm run dev:backend`
5. Health check:
   - `GET http://localhost:5000/health`
